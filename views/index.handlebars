<h1>Eat-Da-Burger!</h1>

<ul>
  {{#each burgers}}
 <li>
    <p>
      {{this.id}}. {{this.burger_name}}

      <button data-burgid="{{this.id}}" class="devburger">Devour It!</button>
    </p>
  </li>
  {{/each}}
</ul>



<form class="create-form">

  <div class="form-group">
    <textarea id="burger_name" name="burger_name" rows="8" cols="40"></textarea>
  </div>

  <button type="submit">Submit</button>
  <label for="burger_name">Enter a burger you want to eat! Click the "Submit" button.</label>
</form>


<script type="text/javascript">
 
  $(".create-form").on("submit", function(event) {
    event.preventDefault();

    let newBurger = {
      burger_name: $("#burger_name").val().trim()
    };

    // Send the POST request.
    $.ajax("/api/burger", {
      type: "POST",
      data: newBurger
    }).then(
      function() {
        console.log("added new burger");
        location.reload();
      }
    );
  });

  $("#devburger").on("submit", function(event)  {
    event.preventDefault();

    let id = $("[name=id]").val().trim();

    let updatedPlan = {
      plan: $("#devourburger [name=burger]").val().trim()
    };

    // Send the PUT request.
    $.ajax("/api/burger/" + id, {
      type: "PUT",
      data: devourBurger
    }).then(
      function() {
        console.log("devoured ", id);
        location.reload();
      }
    );
  }); 
</script>
